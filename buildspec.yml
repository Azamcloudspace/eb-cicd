version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo Installing dependencies...
      - npm install
  build:
    commands:
      - echo Packaging application...
      - zip -r app.zip . -x "*.git*" "node_modules/*"

artifacts:
  files:
    - app.zip
